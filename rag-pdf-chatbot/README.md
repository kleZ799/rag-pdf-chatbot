# RAG PDF Chatbot

A simple Retrieval-Augmented Generation (RAG) project.

## How to Run
1. Install dependencies:
   pip install -r requirements.txt

2. Add your OpenAI API key:
   export OPENAI_API_KEY=your_key

3. Run:
   python app.py

## Note
API key is required to actually run queries.
